# SmartDNS для Brawl Stars

SmartDNS сервер для обхода региональных блокировок Brawl Stars на Android и iOS.

## 🚀 Быстрый старт

### 1. Арендуй VPS

Рекомендуемые провайдеры (оплата из РФ):

| Провайдер                        | Цена      | Локация              |
| -------------------------------- | --------- | -------------------- |
| [Aeza.net](https://aeza.net)     | ~150₽/мес | Германия, Нидерланды |
| [PQ.hosting](https://pq.hosting) | ~3€/мес   | Нидерланды           |
| [Fornex](https://fornex.com)     | ~3€/мес   | Германия             |

**Требования:** Ubuntu 22.04, 512MB RAM, 1 vCore

### 2. Установка на сервер

Подключись к серверу по SSH:

```bash
ssh root@ВАШ_IP_АДРЕС
```

Скачай и запусти скрипт установки:

```bash
wget https://raw.githubusercontent.com/YOUR_GITHUB/smartdns/main/install.sh
chmod +x install.sh
./install.sh
```

Или вручную:

```bash
# Скопируй содержимое install.sh и выполни в терминале
```

### 3. Настройка устройств

#### 📱 Android

**Вариант А: Приложение DNS Changer**

1. Скачай [DNS Changer](https://play.google.com/store/apps/details?id=com.frostnerd.dnschanger)
2. Укажи DNS сервер: `ВАШ_IP_VPS`
3. Включи и запусти Brawl Stars

**Вариант Б: Настройки Wi-Fi**

1. Настройки → Wi-Fi → Долгое нажатие на сеть → Изменить
2. Расширенные настройки → IP: Статический
3. DNS 1: `ВАШ_IP_VPS`

#### 🍎 iOS

**Для Wi-Fi:**

1. Настройки → Wi-Fi → (i) рядом с сетью
2. Настройте DNS → Вручную
3. Удали старые DNS, добавь: `ВАШ_IP_VPS`

**Для мобильного интернета:**

1. Скачай [DNSCloak](https://apps.apple.com/app/dnscloak/id1452162351)
2. Добавь свой DNS сервер
3. Активируй

---

## 📁 Структура проекта

```
dns/
├── docker-compose.yml    # Docker конфигурация
├── install.sh            # Скрипт автоустановки
├── domains.txt           # Список доменов
├── README.md             # Эта инструкция
├── coredns/
│   └── Corefile          # Конфигурация DNS
└── sniproxy/
    └── config.yaml       # Конфигурация SNI Proxy
```

---

## 🔧 Управление сервером

```bash
# Статус контейнеров
docker compose -f /opt/smartdns/docker-compose.yml ps

# Просмотр логов
docker compose -f /opt/smartdns/docker-compose.yml logs -f

# Перезапуск
docker compose -f /opt/smartdns/docker-compose.yml restart

# Остановка
docker compose -f /opt/smartdns/docker-compose.yml down

# Запуск
docker compose -f /opt/smartdns/docker-compose.yml up -d
```

---

## 🧪 Тестирование

Проверка DNS с сервера:

```bash
# Установка dig
apt install -y dnsutils

# Тест DNS
dig @127.0.0.1 game.brawlstars.com

# Должен вернуть IP твоего сервера
```

Проверка DNS удалённо (с любого ПК):

```bash
nslookup game.brawlstars.com ВАШ_IP_VPS
```

---

## ❓ Решение проблем

### Игра всё равно заблокирована

1. Проверь что DNS на устройстве указывает на твой сервер
2. Очисти кэш DNS на устройстве (перезагрузка)
3. Возможно нужны дополнительные домены - проверь логи

### Порт 53 занят

```bash
# Отключи systemd-resolved
systemctl stop systemd-resolved
systemctl disable systemd-resolved
echo "nameserver 8.8.8.8" > /etc/resolv.conf
```

### Контейнеры не запускаются

```bash
# Проверь логи
docker compose logs

# Перезапусти Docker
systemctl restart docker
docker compose up -d
```

---

## ⚠️ Важно

- **Не делись IP сервера** с посторонними
- VPS оплачивается ежемесячно
- Supercell может обнаружить прокси (низкий риск)

---

## 📝 Лицензия

MIT License - используй как хочешь
